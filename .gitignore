# --- Secrets ----------------------------------------------------------------
.env
*.env
!.env.example

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
backend_venv/
venv/
.venv/
env/

# --- Django -----------------------------------------------------------------
*.sqlite3
*.sqlite3-journal
/backend/media/
/backend/staticfiles/

# --- Node / frontend --------------------------------------------------------
node_modules/
dist/
dist-ssr/
*.local
.vite/

# --- Editors / OS -----------------------------------------------------------
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.swp

# --- Logs -------------------------------------------------------------------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Local-only source material ---------------------------------------------
# The original raw asset drop. Its frames are already committed (canonically) at
# frontend/public/frames/, and the intro video at frontend/public/video/, so
# this untracked duplicate stays on the author's machine and out of the repo.
# The logo concept is preserved at assets/brand/logo-concept-raw.jpeg.
fonix_assest/
